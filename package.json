{
  "name": "cancip",
  "version": "0.1.222",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit --skipLibCheck && node scripts/prepare-release-output.mjs && node esbuild.config.mjs production",
    "check": "tsc --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "^0.21.5",
    "obsidian": "1.12.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0",
    "pinyin-pro": "^3.28.1"
  }
}
