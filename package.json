{
  "name": "cancip",
  "version": "2.6.12",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "node scripts/prepare-release-output.mjs && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "check": "node scripts/prepare-release-output.mjs && tsc --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "^0.21.5",
    "obsidian": "1.12.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0",
    "pinyin-pro": "^3.28.1"
  }
}
